================================================================================
  FIX: Vercel login "Unable to connect"
================================================================================

PROBLEM
-------
  Vercel site calls API → api.kaamsetu.app (does NOT exist)
  Render has placeholder "Hello World" only — NOT KaamSetu Django yet

FIXED ON VERCEL
---------------
  - Removed broken VITE_API_URL
  - Added proxy: /api → https://kaamsetu-api.onrender.com/api

YOU MUST DO (one time) — Deploy Django on Render
-------------------------------------------------

1) GitHub par code upload karo:
   - github.com → New repository → kaamsetu
   - Upload F:\KaamSetu folder (without .env, venv, node_modules)

2) Render.com → login → Web Service "kaamsetu-api"
   - Settings → Connect your GitHub repo
   - Root Directory: backend
   - Build Command: bash build.sh
   - Start Command: gunicorn fixmitra.wsgi:application --bind 0.0.0.0:$PORT
   - OR use Blueprint: render.yaml from repo root

3) Render Environment Variables:
   DEBUG=False
   ALLOWED_HOSTS=kaamsetu-api.onrender.com,.onrender.com
   FRONTEND_URL=https://frontend-zeta-ten-20.vercel.app
   CORS_ALLOWED_ORIGINS=https://frontend-zeta-ten-20.vercel.app,https://kaamsetu.app
   DATABASE_URL=(auto from PostgreSQL)
   + Gmail SMTP vars from your .env

4) Add PostgreSQL database on Render (free)

5) Manual Deploy → wait 5-10 min

6) Test: https://kaamsetu-api.onrender.com/api/services/categories/
   Should return JSON (not "Hello, World!")

7) Then Vercel login works:
   https://frontend-zeta-ten-20.vercel.app/login

NOW (until Render deploy)
-------------------------
  Use on your PC:  http://localhost:5173/login
  (backend + frontend must be running)

================================================================================
