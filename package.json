{
  "name": "kaamsetu",
  "private": true,
  "description": "KaamSetu — run scripts from project root",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python kaamsetu.py",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm install --prefix frontend && python kaamsetu.py --help 2>nul || echo Backend: activate venv and pip install -r backend/requirements.txt"
  }
}
